# The Mini Data Structure Library

The `libds` is a mini data structure library. Practising programming is the only reason I made this toy library.

## How To Use

Compile your code using this library with the command below.

```
cd libds      # Get in the libds directory
gcc -I include [OPTIONS] *.c <SOURCE FILE PATH> -o build/main
```